# Backend Environment Configuration
PORT=7000
NODE_ENV=production

# MongoDB Connection String (Replace with your live MongoDB Atlas URI or local MongoDB)
MONGODB_URI=mongodb://127.0.0.1:27017/scripown

# Secure JWT Secret for authentication tokens
JWT_SECRET=change_this_to_a_secure_random_secret_in_production
